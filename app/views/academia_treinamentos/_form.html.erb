<%= form_for(@academia_treinamento) do |f| %>
  <% if @academia_treinamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academia_treinamento.errors.count, "error") %> prohibited this academia_treinamento from being saved:</h2>

      <ul>
      <% @academia_treinamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :local %><br>
    <%= f.text_field :local %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
