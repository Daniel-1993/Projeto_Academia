= simple_form_for(@qualificacao) do |f|
  = f.error_notification

  .form-inputs
    = f.input :comentario
    = f.input :nota
    = f.association :academia_treinamento, label_method: :nome

  .form-actions
    = f.button :submit , "Salvar"
